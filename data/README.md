# Local Data

โฟลเดอร์นี้ใช้เก็บข้อมูลตลาดในเครื่องและถูก ignore โดย Git ยกเว้นไฟล์คำอธิบายนี้

แต่ละชุดข้อมูลที่ใช้จริงต้องบันทึก provider, symbols, fields, timezone, adjustment method, download time และช่วงวันที่ไว้ใน backtest report ห้ามเก็บ credentials หรือ absolute paths ใน repo
