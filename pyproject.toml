[project]
name = "yuehua-camellia"
version = "0.1.0"
description = "A lightweight strategy replication, backtest, and paper-trading research project"
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy>=2.4,<3",
    "pandas>=3.0,<4",
    "yfinance>=1.2,<2",
]

[tool.unittest]
start-directory = "tests"
